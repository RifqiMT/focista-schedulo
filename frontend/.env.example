# Optional: production API origin (no trailing slash). Required when the SPA is hosted
# separately from the Express API — e.g. Vercel frontend + Railway API.
# VITE_API_BASE_URL=https://api.example.com
