# Optional: deployed backend origin (no trailing slash). Required when the UI is hosted
# on a different origin than the API (e.g. Vercel UI + Render/Fly API).
# REQUIRED on Vercel Production builds (VERCEL_ENV=production).
# VITE_API_BASE_URL=https://your-api-host.example.com
